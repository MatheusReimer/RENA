/** Defaults for every paginated endpoint (SPEC 38). */
export const PAGE_SIZE_DEFAULT = 20
export const PAGE_SIZE_MAX = 50

/** Feed pages are smaller because each item carries media artwork. */
export const FEED_PAGE_SIZE_DEFAULT = 15
