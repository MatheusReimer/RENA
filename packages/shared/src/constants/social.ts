/** Friendship lifecycle states (SPEC 12). */
export const FRIENDSHIP_STATUSES = ['pending', 'accepted', 'rejected', 'blocked'] as const

/** List visibility (SPEC 15). */
export const LIST_VISIBILITIES = ['private', 'friends', 'public'] as const
